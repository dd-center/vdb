{
  "scripts": {
    "test": "node test/test",
    "update": "npm test && npm run generate && sh update.sh",
    "generate": "node generator",
    "refresh": "node refresh"
  },
  "dependencies": {
    "uuid": "^3.3.2"
  },
  "devDependencies": {
    "got": "^9.6.0",
    "inquirer": "^7.0.0"
  }
}
