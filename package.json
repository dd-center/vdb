{
  "scripts": {
    "test": "node test/test",
    "update": "npm test && npm run generate && sh update.sh ; exit 0",
    "generate": "node generator",
    "refresh": "node refresh"
  },
  "dependencies": {
    "uuid": "^7.0.0"
  },
  "devDependencies": {
    "dugite": "^1.88.0",
    "got": "^10.0.1",
    "inquirer": "^7.0.0"
  }
}
