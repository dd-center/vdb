{
  "scripts": {
    "test": "node test/test",
    "update": "npm test && npm run generate && sh update.sh ; exit 0",
    "generate": "node generator",
    "refresh": "node refresh"
  },
  "dependencies": {
    "uuid": "^3.3.2"
  },
  "devDependencies": {
    "cross-spawn": "^7.0.1",
    "got": "^10.0.1",
    "inquirer": "^7.0.0"
  }
}
