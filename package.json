{
  "name": "vdb",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "test": "node test/test",
    "action-test": "node test/action-test",
    "update": "npm test && npm run generate ; exit 0",
    "generate": "node generator.mjs",
    "refresh": "node refresh"
  },
  "dependencies": {
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@actions/core": "^1.2.4",
    "@actions/github": "^5.1.1",
    "@types/uuid": "^9.0.8",
    "dugite": "^2.5.1"
  }
}
