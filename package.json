{
  "name": "vdb",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "test": "node test/test",
    "action-test": "node test/action-test",
    "update": "npm test && npm run generate && sh update.sh ; exit 0",
    "generate": "node generator",
    "refresh": "node refresh"
  },
  "dependencies": {
    "uuid": "^7.0.1"
  },
  "devDependencies": {
    "@actions/core": "^1.2.4",
    "@types/uuid": "^8.0.1",
    "dugite": "^1.88.0",
    "got": "^11.0.2",
    "inquirer": "^7.0.0"
  }
}
